conditioning:
  main_sampler: resample
  method: ps # Do not touch
  params:
    scale: 0.1 # Try changing this

data:
  name: custom
  root: ./data/samples_psmc_512/final_ones_512

measurement:
  operator:
    name: super_resolution
    in_shape: !!python/tuple [1, 3, 512, 512]
    scale_factor: 2
  
  noise: 
    name: gaussian
    sigma: 0.0
